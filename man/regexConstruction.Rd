% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Regex.R
\name{regexConstruction}
\alias{regexConstruction}
\alias{captureRE}
\alias{captureUniq}
\alias{orRE}
\title{Making Regular Expressions}
\usage{
captureRE(strs, n = "")

captureUniq(strs, zero = TRUE)

orRE(...)
}
\description{
\code{humdrumR} includes some helpful functions for creating new regular expressions which work with the
\link{stringr} package.
}
\details{
\code{captureRE} will take a character vector and collapse it to a "capture group."
The \code{n} argument can be used to append a number tag, for instance \code{'*'} (zero or more) to the group.
I.e., \code{captureRE(c("a", "b", "c"), '*')} will output \code{"[abc]*"}.

\code{captureUniq} will make a similar capture group to \code{captureRE}, but with an expression
that makes sure that only 1 or more \emph{of the same character} repeats.
For instance, \code{captureUniq(c('a', 'b','c'))} will return \code{"([abc])\\\\1*"}---this expression will match
\code{"aaa"} or \code{"bb"} but not \code{"aabb"}.
}
