% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plots.R
\name{drawMultiple}
\alias{drawMultiple}
\alias{drawBeside}
\alias{drawBelow}
\alias{drawNothing}
\title{Draw multiple plots next to each other.}
\usage{
drawBeside(..., widths = NULL)

drawBelow(..., heights = NULL)

drawNothing()
}
\arguments{
\item{...}{\emph{\strong{Plots to combine.}}

Must be \code{plot_object}s, created by \code{\link[=draw]{draw()}}.}

\item{widths}{\emph{\strong{How should horizontal space be divided up between plots?}}

Defaults to \code{NULL}, leading to equal spacing.}

\item{heights}{\emph{\strong{How should vertical space be divided up between plots?}}

Defaults to \code{NULL}, leading to equal spacing.}
}
\description{
Draw multiple plots next to each other.
}
\details{
Use this function to draw multiple, aribitrary \code{\link[=draw]{draw()}} plots.
The \code{...} arguments can be one or more \code{\link[=draw]{draw()}} plots, which are placed
beside (or below) each other.

The \code{heights} or \code{widths} arguments can be used control the relative height (\code{drawBelow})
or width (\code{drawBeside}) of the plots.

\code{drawNothing()} can be used to put an empty space in a layout.
}
\examples{

x1 <- rnorm(200, mean = 10, sd = 10)
x2 <- rnorm(200, mean = 10, sd = 10)

draw(x1) |> drawBeside(x2)
draw(x1) |> drawBelow(x2)

draw(x1, title = 'Histogram of x1') |> drawBeside(draw( ,x1, title = 'Q-plot of x1'))

draw(x1, title = 'Histogram of x1') |> drawBeside(draw( ,x1, title = 'Thinner Q-plot of x1'), widths = c(2,1))

draw(x1) |> drawBeside(draw(x2)) |> drawBeside(draw(x1, x2))

draw(x1) |> drawBeside(draw(x2), draw(x1, x2)) 

draw(x1) |> drawBeside(draw(x2)) |> drawBelow(draw(x1, x2))

# Using draw nothing

drawBeside(draw(x1), draw(, x1)) |> drawBelow(drawBeside(draw(x2), drawNothing() |> drawMore(text(.5, .5, "Blank space"))))

}
\seealso{
Combines plots created with \code{\link[=draw]{draw()}}. Use \code{\link[=drawMore]{drawMore()}} to \emph{add} to existing plots.
}
