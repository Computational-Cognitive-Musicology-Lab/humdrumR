% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pitch.R
\name{is.simple}
\alias{is.simple}
\alias{is.simple.tonalInterval}
\alias{is.simple.default}
\alias{is.generic}
\alias{is.generic.tonalInterval}
\title{Test the properties of tonal information}
\usage{
is.simple(x, ...)

\method{is.simple}{tonalInterval}(x, octave.round = floor, ...)

\method{is.simple}{default}(x, ...)

is.generic(x, Key, ...)

\method{is.generic}{tonalInterval}(x, Key = NULL)
}
\arguments{
\item{x}{\emph{\strong{Pitch information.}}

Must be something that can be \link[=pitchParsing]{parsed as pitch information}.}

\item{...}{\emph{\strong{Parameters passed to \code{\link[=tonalInterval]{tonalInterval()}}.}}}

\item{octave.round}{\emph{\strong{The rounding function.}}

Must be a \link[=expand]{rouding function}.

Controls how simple intervals are interpreted relative to C.}

\item{Key}{\emph{\strong{The diatonic key used to defined generic pitches.}}

Defaults to \code{NULL}.

Must be something that can be \link[=keyParsing]{parsed as a diatonic key}; must be either length \code{1} or \code{length(x)}.}
}
\description{
These functions test basic properties of pitch information.
\code{is.simple} returns \code{TRUE} if pitch information is constrained
in one octave.
\code{is.generic} returns \code{TRUE} if pitch information is "natural" to
the key (\code{Key}) argument.
}
\details{
These functions can be called directly on \link[=tonalIntervalS4]{tonalIntervals};
If called on anything else, the functions first calls the \code{\link[=tonalInterval]{tonalInterval()}}
parser. If any values fail to parse \code{NA} is returned.

"Simple" intervals fall in a particular octave relative to middle-C/unison.
The \code{octave.floor} argument can be used to change how this works:
The default option, \code{floor}, interprets the (**kern) pitches  \code{c}, \code{d}, \code{e}, \code{f}, \code{g}, \code{a}, and \code{b} to be "simple."
The most common alternative, \code{round}, identifies \code{G}, \code{A}, \code{B}, \code{c}, \code{d}, \code{e}, and \code{f} as "simple."
See the \link[=pitchDeparsing]{pitch deparsing docs} for a more detailed explanation.

"Generic" intervals belong to a key.
}
\seealso{
Other {Tonal feature functions}: 
\code{\link{is.major}()}
}
\concept{{Tonal feature functions}}
