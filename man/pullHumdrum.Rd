% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/humdrumR-class.R
\name{pullHumdrum}
\alias{pullHumdrum}
\alias{pull_data.table}
\alias{pull_data.frame}
\alias{pull_tibble}
\alias{pull.humdrumR}
\alias{pull}
\alias{$,humdrumR-method}
\title{Extract field(s) from \link[=humdrumRclass]{humdrumR data}}
\usage{
pull_data.table(humdrumR, ..., dataTypes = "D", null = "charNA2dot")

pull_data.frame(humdrumR, ..., dataTypes = "D", null = "charNA2dot")

pull_tibble(humdrumR, ..., dataTypes = "D", null = "charNA2dot")

\method{pull}{humdrumR}(.data, var, dataTypes = "D", null = "asis")

\S4method{$}{humdrumR}(x, name)
}
\arguments{
\item{humdrumR, .data, x}{\emph{\strong{HumdrumR data.}}

Must be a \link[=humdrumRclass]{humdrumR data object}.}

\item{...}{\emph{\strong{Which fields to output.}}

If no arguments are provided, the object's \link[=selectFields]{selected fields} are pulled.

These arguments can be any combination of \code{character} strings, numbers, or symbols used
to match fields in the \code{humdrumR} input using \link[dplyr:select]{tidyverse} semantics.

Unlike in tidyverse \code{select()}, field names can be \link[=partialMatching]{partially matched}.
You can also include \code{character} strings \link[=partialMatching]{partially matching}
\code{"Data"}, \code{"Structure"}, \code{"Interpretation"}, \code{"Formal"}, \code{"Reference"} or \code{"Grouping"},
which will select all fields of those types (see \code{\link[=fields]{fields()}} for further explanation).}

\item{dataTypes}{\emph{\strong{Which types of humdrum record(s) to include.}}

Only non-null data tokens (\code{"D"}) are returned by default.

Must be a single \code{character} string. Legal values are \verb{'G', 'L', 'I', 'M', 'D', 'd'}
or any combination of these (e.g., \code{"LIM"}).
(See the \link[=humTable]{humdrum table} documentation for explanation.)}

\item{null}{\emph{\strong{How should null data points be output?}}

Default is \code{"charNA2dot"}.

Must be a single character string, \link[=partialMatchng]{partially matching} \code{"NA2dot"}, \code{"dot2NA"}, \verb{'charNA2dot"}, or \code{"asis"}.}

\item{var}{\emph{\strong{Which field to output.}}

Defaults to \code{selectedFields(humdrumR)[1]}.

Must be either a single \code{character} string or \code{symbol} which \link[=partialMatching]{partially matches}
a field name, or a single whole-number, which selects the field by the row index of the \code{\link[=fields]{fields()}} output.
If a negative number is provided, nth-to-last index is used---for example, \code{-1} would grab the last field.}
}
\description{
Individual fields from the \link[=humTable]{humdrum table} can be extracted using \code{pull()}.
Multiple fields can be extracted using \code{pull_data.frame()}, \code{pull_data.table}, or \code{pull_tibble()}
---the resulting data.frames are a column-subset of the humdrum table.
You can also use the \code{$} operator to extract a single field, just like \code{pull()}.
}
\details{
The functions, \code{pull_data.___}, \code{pull()}, and \verb{$.humdrumR} are the "escape hatch" to pull your
data out of the \link[=humdrumRclass]{humdrumR data world} into "normal" R.
The \code{pull()} function (and the \code{$}) method, the actual vector content of a (single) field.
The other functions return \emph{always} return a \code{data.frame}/\code{data.table}/\code{tibble}, even if it has only one column.
If no fields are indicated, the data's \link[=selectedFields]{selected fields} are pulled; in the case of \code{pull()},
only the \emph{first} seleced field is pulled.

The \code{dataTypes} argument controls which types of data are pulled---by default, only non-null data is returned.
The \code{null} argument controls how null data is returned, with four options:
\itemize{
\item \code{"NA2dot"} means all \code{NA} values are converted to \code{"."}; note that this will cause all output to be coerced to \code{character}.
\item \code{"dot2NA"} means all \code{"."} are converted to \code{NA}.
\item \code{"charNA2dot"} means \code{NA} values in \code{character} vectors are converted to \code{NA}, but not in other atomic types.
\item \code{"asis"} means either \code{NA} or \code{"."} values may print, depending on what is in the field.
}

Note that \code{pull_tibble()} won't work if you don't independently load the \code{tibble} (or \code{tidyverse}) package---
i.e., call \code{library(tibble)}.
}
\examples{

humData <- readHumdrum(humdrumRroot, "HumdrumData/BachChorales/chor00[1-4].krn")

humData |> pull(Token)
humData$Token

humData |> pull_data.table(Token, Spine)
humData |> pull_tibble(everything())
}
\seealso{
{To know what fields are available to pull, use \code{\link[=fields]{fields()}}.
To know what fields are selected---the default fields to pull---use \code{\link[=selectedFields]{selectedFields()}}.}
}
