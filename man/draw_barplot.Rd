% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plots.R
\name{draw_barplot}
\alias{draw_barplot}
\title{Draw bar plot}
\usage{
draw(x # table/counts, 
     col = NA)
     
draw( , y # table/counts, 
     col = NA)
}
\arguments{
\item{log}{\emph{\strong{Should X and/or Y axes be drawn on a logarithmic scale?}}

Defaults to \code{""} (linear scale on both axes).

Must be a single \code{character} string; options are \code{"x"} (X axis on log scale),
\code{"y"} (Y axis on log scale), and \code{"xy"} (both axes on log scale).}

\item{heat}{\emph{\strong{Should a heatmap be drawn?}}

Defaults to \code{FALSE}, unless the input has two or more dimensions
\emph{and} at least 80 conditions.

Must be a singleton \code{logical} value: an on/off switch.}

\item{quantiles}{\emph{\strong{Should distribution quantiles of \code{x} and \code{y} be marked?}}

Defaults to \code{c()}, so no quantiles are drawn.

Must be a vector of numbers between 0 and 1 (inclusive), or an empty vector (the default).

If any quantiles are specified, each quantile is drawn on the plot and labeled appropriately.
Quantiles are computed separately for input vectors \code{x} and \code{y} and drawn using vertical and horizontal
lines respectively.
For example, \code{quantiles = .5} will draw a lines which converge at the medians of \code{x} and \code{y};
\code{quantiles = c(.25, .5, .75)} will draw vertical and horizontal lines marking the four
quartiles of \code{x} and \code{y} (creating a grid with 16 cells).}

\item{mean}{\emph{\strong{Should the mean of input vectors \code{x} and \code{y} be marked on the plot?}}

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: an on/off switch.}

\item{...}{\emph{\strong{Additional parameters to pass to par().}}

Any base-R graphing parameters that can be set using the \code{\link[=par]{par()}} function
may be passed to \code{draw()}.
These parameters are set using \code{par()} (overriding humdrumR's defaults), but only for the duration of the
\code{draw()} call---i.e., the global \code{par()} settings are not changed.}
}
\description{
This method draws \link[=count]{tabulated data} as a bar plot.
If the data is passed to the first (\code{x}) argument, the bar plot is oriented vertically;
If the data is instead passed to the second (\code{y}) argument----with \code{x} missing---,
the bar plot is oriented horizontally, from left to right.
The input table can have one or two dimensions; if more dimensions are provided
the third and fourth dimension are split across draw facets.
}
\details{
The \code{draw_barplot()} method will accept tabular data created
\code{humdrumR} functions \code{\link[=count]{count()}} and \code{\link[=pdist]{pdist()}}, or equivalent base-R functions \code{\link[=table]{table()}}/\code{\link[=proportions]{proportions()}}.
If a single atomic vector of discrete values is passed to either \code{x} or \code{y}, the discrete
values are automatically \link[=count]{counted}, and the resulting table is passed to \code{draw_barplot()}.
Thus, if \code{mydiscrete} is a vector of discrete values (like \code{character}),
calling \code{draw(mydiscrete)} is the same as calling \code{draw(count(mydiscrete))}.

For count data (natural numbers), Y-axis labels for counts and proportion of total are shown;
For proportion data (real numbers between 0 and 1) only a proportion key is shown;
If the table includes negative numbers, the Y axis is simply labeled "value."
\subsection{Dimensions}{

A single-dimensional table (representing one variable) is simply drawn as a set of bars.
However, if the input table is two dimensional, a double bar plot is drawn, with bars
representing every combination of levels across the two dimensions;
Bars representing values of the first dimension are drawn in groups representing
each level of second dimension.
However, if the total number of bars to draw is greater than 80,
\code{\link[=draw]{draw()}} will dispatch \code{\link[=draw_heat]{draw_heat()}} instead.
This behavior can be overridden using the \code{heat} argument.
}

\subsection{Barplot types}{

By default, bars are drawn side by side, so their relative height is easiest to compare.
However, is \code{beside = FALSE}, a "stacked" bar plot will be drawn, with bars stacked on top of each other.
If \code{beside = NULL}, side-by-side \emph{and} stacked plots are drawn---the bars are drawn side by side,
but then redrawn (with more transparency) stacked on top of the right-most bar.
}
}
\section{Color}{
NA
}

\section{General Draw Arguments}{


\code{draw()} is built on top of R's base plotting system.
This means that all the standard arguments to base-R plots can be used to customize \code{draw()} plots
(see \code{\link[=par]{par()}} for a full list).
You can also add stuff to \code{draw()} plots using base functions like \code{\link[=points]{points()}}, \code{\link[=mtext]{mtext()}}, or \code{\link[=abline]{abline()}}.
\code{draw()} also has a number of unique features, including easily dividing data into multiple plots ("facets").
\subsection{Plot Text}{

Every \code{draw()} plot can have a title, subtitle, X-axis label, and Y-axis label.

The \code{draw()} function will automatically generate X and Y labels for every plot,
usually just using the expression you passed; for example, if you say \code{draw(rnorm(100))}, the
X label will be "rnorm(100)."
This can be overridden using the \code{xlabe;} and/or \code{ylabe;} arguments, which can be provided a single string
each---to suppress a label, provide an empty string, like \code{ylabel = ""}.

Titles and subtitles are specified using the \code{title} and \code{subtitle} arguments, respectively.
(Alternatively, you can use the standard \code{main} and \code{sub} arguments.)
No title or subtitle is drawn by default.

You can use the base-R \code{\link[=mtext]{mtext()}} function to draw additional text on plot axes.
}

\subsection{Axes control}{

The \code{draw()} function will select reasonable X and Y axes ranges automatically.
If you want to override the defaults, you can use \code{xlimit} or \code{ylimit} to control
the range of values shown on each axis.
Each of these must be passed a vector of two numbers, representing the left and right
X-axis extremes (\code{xlimit}) and the bottom and top Y-axis extremes (\code{ylimit}).
For example, to show data in the range \eqn{[10, 50]} on the X axis,
specify \code{xlimit = c(10, 50)}.

For \code{numeric} axes, you can also plot data on a logarithmic scale
using the \code{log} argument.
This is set by providing a single \code{character} string containing a lower-case
\code{"x"}, \code{"y"}, or both (\code{"xy"}).
Note that \code{draw()} will throw an error if you try to plot negative values
on a logarithmic scale.
Also note that some plots will not allow logarithmic scaling on some axes,
and will simply ignore attempts to do that.
}

\subsection{Window control}{

Normally, when you use \code{draw()}, the plot is drawn to fill the available graphical device,
using base-R's normal algorithm.
However, the \code{aspect} argument can override this, controlling the aspect
ratio of the plot.
The \code{aspect} argument must be a single numeric value between \code{0.5} and \code{5}.
If \code{aspect = 1} the plot is drawn as a square.
Try settings like \code{aspect = 4/3} or \code{aspect = 16/9}.

The \code{margin} argument controls the portion of the screen used for the plot margins,
with legal values ranging from \code{0.4} to \code{0.1}.
The default value is \code{0.2}---using other margins may result in less optimal placement
of plot text and legends.
}
}

