% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Metric.R
\name{duple}
\alias{duple}
\title{Generate duple meters}
\usage{
duple(nlevels = 4, measure = 1, tactus = 3L)
}
\arguments{
\item{nlevels}{\emph{\strong{The number of duple levels.}}

Must be a singleton, positive natural number}

\item{measure}{\emph{\strong{The duration of the top level of the meter.}}

Must be a singleton \code{numeric} or \code{character} value.

Is parsed as a duration by \code{\link[=rhythmInterval]{rhythmInterval()}}; a failure to parse leads to an error.}

\item{tactus}{\emph{\strong{Which level is the tactus?}}

Must be a singleton, positive natural number; must be less than or equal to \code{nlevels}.}
}
\description{
This function generates a \code{\link[=meter]{meter()}} objects representing duple meters.
The desired number of duple levels is controlled by the \code{nlevels} argument.
The span of the meter (i.e., the highest level) is indicated by the \code{measure} argument.
Finally, the \code{tactus} argument indicates which level (indexe from highest to lowest)
is the tactus.
The default arguments build a 4/4 meter with levels ranging from whole-notes down
to sixteenth-notes, and a quarter-note tactus.
}
\seealso{
{Use the \code{\link[=meter]{meter()}} function to create abitrary meters.}
}
