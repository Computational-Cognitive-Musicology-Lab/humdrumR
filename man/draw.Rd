% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plots.R
\name{draw}
\alias{draw}
\title{Visualize data}
\usage{
draw(x, y, ...)
}
\arguments{
\item{...}{\emph{\strong{Additional parameters to pass to par().}}

Any base-R graphing parameters that can be set using the \code{\link[=par]{par()}} function
may be passed to \code{draw()}.
These parameters are set using \code{par()} (overriding humdrumR's defaults), but only for the duration of the
\code{draw()} call---i.e., the global \code{par()} settings are not changed.}

\item{xlab, ylab}{\emph{\strong{What X and/or Y axis labels should be used?}}

Must be single \code{character} strings.

See "Plot Text" section below.}

\item{xlimit, ylimit}{\emph{\strong{What range of X/Y values should drawn on the plot?}}

By default, X and Y limits are automatically selected.

Must be a \code{numeric} vector of length two.

The first number of each \code{xlimit}/\code{ylimit} vector specifies the left/bottom
edge of the X/Y axis. The second number specifies the right/top edge.}

\item{legend}{\emph{\strong{Should legends be drawn?}}

Defaults to \code{TRUE}.}

\item{aspect}{\emph{\strong{Control the aspect ratio of the plot.}}

Defaults to \code{NULL}.

Must be a single numeric value \eqn{5 \geq aspect \geq 0.2}, or \code{NULL}.

This controls the aspect ratio of the plot:
\code{1} (square), \code{4/3}, \code{16/9}, etc.
If \code{aspect} is \code{NULL} , R automatically uses the current aspect of
your current plotting device.}

\item{margin}{\emph{\strong{How big should plot margins be?}}

Defaults to \code{0.2}.

Must be as single numeric value \eqn{0.4 \geq margin \geq 0.1}.

This controls the proportion of the plotting area used for margins.
A value of \code{0.2} means that 20\% of the plotting area is used for the margins.}

\item{heat}{\emph{\strong{Should a heatmap be drawn?}}

Defaults to \code{FALSE}, unless the input has two or more dimensions
\emph{and} at least 80 conditions.

Must be a singleton \code{logical} value: an on/off switch.}
}
\description{
The \code{draw()} function is humdrumR's go-to plotting function,
which can make a variety of graphs depending on the type of data you give it.
For the most part, \code{draw()} is just an easy-to-use wrapper around
the base-R graphics functions---Anything that \code{draw()} does can be done
using normal base-R functions (\code{\link[=plot]{plot()}}, \code{\link[=barplot]{barplot()}}, etc.).
However, \code{draw()} makes making good looking plots faster and easier.
}
\details{
\code{draw()} is a generic function, which does different sorts of plots depending on the data you pass to its
\code{x} and \code{y} arguments.
The following table indicates the seven possible plot types, and which combinations of \code{x} and \code{y} variable classes
result in each type.
Separate sections below explaining the detailed features of each type of plot.\tabular{lll}{
   \code{x} \tab \code{y} \tab Plot type \cr
   \code{numeric} \tab (missing) \tab \link[=draw_density]{Density Histogram/Contour} \cr
   (missing) \tab \code{numeric} \tab \link[=draw_Qplot]{Quantile plot} \cr
    \tab  \tab (or Violin plot) \cr
   \code{numeric} \tab \code{numeric} \tab \link[=draw_scatter]{Scatter/line plot} \cr
   \code{character}/\code{factor} \tab \code{numeric} \tab \link[=draw_violins]{Violin plot} \cr
   \code{numeric} \tab \code{character} or \code{factor} \tab \link[=draw_area]{Area chart} \cr
   \code{character}/ \code{factor} \tab (missing) \tab \link[=draw_barplot]{barplot} \cr
   (missing) \tab \code{character}/\code{factor} \tab \link[=draw_barplot]{barplot} \cr
   \code{character}/\code{factor} \tab \code{character}/\code{factor} \tab \link[=draw_heat]{Heat map} \cr
}

\subsection{Drawing dimensions of data}{

\code{draw()} is equipped to visualize data in up to five dimensions in one plot.
The main dimensions are, of course, the X and Y axes, controlled by the \code{x} and \code{y} arguments.
The other two dimensions are color (\code{col}), point size (\code{cex}), and point shape (\code{pch}).
Another approach is to draw multiple plots at the same time in a grid, with each sub
plot called a "facet."
Details for all these options can be found below.
}
}
\section{General Draw Arguments}{


\code{draw()} is built on top of R's base plotting system.
This means that all the standard arguments to base-R plots can be used to customize \code{draw()} plots
(see \code{\link[=par]{par()}} for a full list).
You can also add stuff to \code{draw()} plots using base functions like \code{\link[=points]{points()}}, \code{\link[=mtext]{mtext()}}, or \code{\link[=abline]{abline()}}.
\code{draw()} also has a number of unique features, including easily dividing data into multiple plots ("facets").
\subsection{Plot Text}{

Every \code{draw()} plot can have a title, subtitle, X-axis label, and Y-axis label.

The \code{draw()} function will automatically generate X and Y labels for every plot,
usually just using the expression you passed; for example, if you say \code{draw(rnorm(100))}, the
X label will be "rnorm(100)."
This can be overridden using the \code{xlabe;} and/or \code{ylabe;} arguments, which can be provided a single string
each---to suppress a label, provide an empty string, like \code{ylabel = ""}.

Titles and subtitles are specified using the \code{title} and \code{subtitle} arguments, respectively.
(Alternatively, you can use the standard \code{main} and \code{sub} arguments.)
No title or subtitle is drawn by default.

You can use the base-R \code{\link[=mtext]{mtext()}} function to draw additional text on plot axes.
}

\subsection{Axes control}{

The \code{draw()} function will select reasonable X and Y axes ranges automatically.
If you want to override the defaults, you can use \code{xlimit} or \code{ylimit} to control
the range of values shown on each axis.
Each of these must be passed a vector of two numbers, representing the left and right
X-axis extremes (\code{xlimit}) and the bottom and top Y-axis extremes (\code{ylimit}).
For example, to show data in the range \eqn{[10, 50]} on the X axis,
specify \code{xlimit = c(10, 50)}.

For \code{numeric} axes, you can also plot data on a logarithmic scale
using the \code{log} argument.
This is set by providing a single \code{character} string containing a lower-case
\code{"x"}, \code{"y"}, or both (\code{"xy"}).
Note that \code{draw()} will throw an error if you try to plot negative values
on a logarithmic scale.
Also note that some plots will not allow logarithmic scaling on some axes,
and will simply ignore attempts to do that.
}

\subsection{Window control}{

Normally, when you use \code{draw()}, the plot is drawn to fill the available graphical device,
using base-R's normal algorithm.
However, the \code{aspect} argument can override this, controlling the aspect
ratio of the plot.
The \code{aspect} argument must be a single numeric value between \code{0.5} and \code{5}.
If \code{aspect = 1} the plot is drawn as a square.
Try settings like \code{aspect = 4/3} or \code{aspect = 16/9}.

The \code{margin} argument controls the portion of the screen used for the plot margins,
with legal values ranging from \code{0.4} to \code{0.1}.
The default value is \code{0.2}---using other margins may result in less optimal placement
of plot text and legends.
}
}

\section{Color}{


Colors can be specified in all \code{draw()} plots using the \code{color} argument, along with the
\code{alpha} argument which controls the transparency of colors.
Note that color control can used for entirely aesthetic purposes (picking
a color scheme you want) \emph{or} to represent an additional dimension of data.

Colors can be specified several ways:
\itemize{
\item As names (e.g., \code{"red"} or \code{"darkgreen"}).
\item As \href{https://en.wikipedia.org/wiki/Web_colors#hex-triplet}{hex codes} (e.g., \code{"#ff0000"} or \verb{"#00ff00}).
\item Using the \code{\link[=rgb]{rgb()}} function.
\item As natural number (1,2,3, etc.), indexing \code{humdrumR}'s flatly palette,
based on the colors \code{'#18BC9C'}, \code{'#3498DB'}, \code{'#F39C12'}, \code{'#E74C3C'}, and \code{'#2C3E50'}.
}

The \code{alpha} argument must be a \code{numeric} value \eqn{1 \geq alpha \geq 0},
where \code{alpha = 0} is totally transparent and \code{alpha = 1} is totally opaque.

It is always possible to specify a single color value for a plot.
However, \code{draw()} can also (generally) accept more color values, depending on the type of plot.
For some plots, multiple colors are used (aesthetically) by default;
For other types of plots, its possible to use color to represent an additional dimension of information.
In the "Specific Plot Types" subsections below, the details of how each plot type interprets
the \code{col} argument are explained.
}

\section{Facets}{

}

\seealso{
Use \code{\link[=drawToFile]{drawToFile()}} to render these plots to files.
}
