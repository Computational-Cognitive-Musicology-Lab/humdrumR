% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Summary.R
\name{interpretations}
\alias{interpretations}
\alias{print.humInterpretations}
\title{Summarize humdrum corpus interpretations.}
\usage{
interpretations(humdrumR)

\method{print}{humInterpretations}(interps, showEach = TRUE, screenWidth = options("width")$width - 10L)
}
\arguments{
\item{humdrumR}{A \link[=humdrumR-class]{humdrumR} data object.}
}
\description{
\code{interpretations} is one of \link{humdrumR}'s \link[=humSummary]{summary functions},
used to summarize the interpretations in the pieces of a humdrumR corpus,
including \emph{exclusive} (\verb{**}) and \emph{tandem} (\code{*}) interpretations.
It returns a \code{humInterpretations} argument which prints as a informative table in the terminal.
}
\details{
Each row in the output table represents a single piece in the corus.
The first column (\code{(n)}) indicates a unique "exclusive pattern" associated with
each piece---the exclusive patterns are tallied at the bottom.
The remaining columns indicate how many of each interpretation (indicated by column name)
appear in each piece.

For tandem interpretations, counts are returned in the format \code{Total.Unique.Spines}:
\itemize{
\item \code{Total}: The total instances of this interpretation, across all spines.
\item \code{Unique}: The number of unique versions of this interpretation.
\item \code{Spines}: The number of spines that this interpretation appears in.
}

For example, consider the following file:\preformatted{**kern   **kern   **silbe
   *C:      *C:         *
     c        e        La
     d        f        la
     e        g        la
   *e:      *e:         *
    f#       d#        la
     g        e         _
     a        b         _
   *G:      *G:         *
    f#        a       doo
     g        b       wop
    *-       *-        *-
}

In this file, there is several tandem key interpretations,
which \code{humdrumR} will call \code{Key}.
The tabulation by \code{interpretations} will return a \code{Key} column with the value
\verb{6.3.2} for this file:
\itemize{
\item \code{6} because there are six key interpretations in total.
\item \code{3} because there are three unique keys: \verb{*C:}, \verb{*e:} and \verb{*G:}.
\item \code{2} because the key interpretations only occur in two spines.
}
}
\seealso{
Other corpus summary functions: 
\code{\link{census}()},
\code{\link{humSummary}},
\code{\link{reference}()},
\code{\link{spines}()}
}
\concept{corpus summary functions}
