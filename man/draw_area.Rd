% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plots.R
\name{draw_area}
\alias{draw_area}
\title{Draw area plot}
\usage{
draw(x # numeric,  
     y # discrete,
     col = NA # colors chosen automatically)
}
\arguments{
\item{log}{\emph{\strong{Should X axis be drawn on a logarithmic scale?}}

Defaults to \code{""} (linear scale).

Must be a single \code{character} string, either \code{""} (linear scale)
of \code{"x"} (draw X on a logarithmic scale ).}

\item{center}{\emph{\strong{Should the stacked areas be centered on the screen?}}

Defaults to \code{TRUE}.

Must be a singleton \code{logical} value: an on/off switch.

If \code{TRUE}, the areas are centered in the Y-axis, expanding outward (up and down)
when the density increases.
If \code{FALSE}, the areas are stacked up from the bottom of the screen.}

\item{smooth}{\emph{\strong{Should a smoothed density curve be estimated for each group?}}

Defaults to \code{TRUE}.

Must be a singleton \code{logical} value: an on/off switch.

If \code{TRUE}, a smooth density contour is estimated from each group of \code{x} values, using \code{\link[=density]{density()}}.
If \code{FALSE}, \code{x} values are binned using \code{\link[=hist]{hist()}}.}

\item{conditional}{\emph{\strong{Should probability density be calculated separately within each group?}}

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: an on/off switch.

This controls whether the density
of color area matches its global share in the distribution of input variable \code{x}, or is rescaled in each
group to sum/integrate to 1. I.e., should probabilities be conditioned on the grouping factor?
Setting \code{conditional = TRUE} is useful if you want to see the details of how each group is distributed.
\code{conditional = FALSE} (the default) is useful when you want to see the actual proportion of
data in each group (i.e., if the groups are different sizes).}

\item{mean}{\emph{\strong{Should the mean of input vector \code{x} be marked below the plot?}}

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: an on/off switch.}

\item{quantiles}{\emph{\strong{Should distribution quantiles of \code{y} be marked?}}

Defaults to \code{c()}, so no quantiles are drawn.

Must be a vector of numbers between 0 and 1 (inclusive), or an empty vector.

If any quantiles are specified, each quantile is drawn as a vetical line on the plot, labeled appropriately.
For example, \code{quantiles = .5} will draw a line at the median of input vector \code{x};
\code{quantiles = c(.25, .5, .75)} will draw lines marking the four quartiles of \code{x}.
(Depends on \code{global_stats} argument.)}

\item{showPoints}{\emph{\strong{Should individual points from \code{x} be shown above the density plot?}}

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: an on/off switch.

If \code{TRUE}, individual data points from the \code{x} input vector
are plotted in a "cloud" above the area plot.
The X-position of each point is matched to its actual value;
The Y-position of each point is randomly (uniformly) selected in a range at the top of the plot window.
This spaces out points that are close together, so it is easier to see how dense they are.}

\item{...}{\emph{\strong{Additional parameters to pass to par().}}

Any base-R graphing parameters that can be set using the \code{\link[=par]{par()}} function
may be passed to \code{draw()}.
These parameters are set using \code{par()} (overriding humdrumR's defaults), but only for the duration of the
\code{draw()} call---i.e., the global \code{par()} settings are not changed.}

\item{global_stats}{\emph{\strong{Should quantiles and/or means be drawn within each group?}}

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: an on/off switch.

If \code{global_stats = FALSE}, quantiles and/or means are drawn separately for each group.
This can get very messy very quickly!}
}
\description{
This method draws the distribution of a categorical (discrete) \code{y} variable
across the range of a numeric (continuous) \code{x} variable, creating an
"\href{https://en.wikipedia.org/wiki/Area_chart}{area plot}."
Specifically, colored areas representing the estimated probability density of each
unique category in \code{y}, depending on the value of \code{x}, are stacked on top of each
other.
The result is like stacking multiple histograms on top of each other.
}
\details{
Like \code{\link[=draw_density]{draw_density()}}, the area-plot density shape(s) can be
generated using either R's \code{\link[=density]{density()}} or
\code{\link[=hist]{hist()}} algorithms, for smooth and binned plots respectively.
We can pass arguments directly through to these functions:
For example, the \code{breaks} argument can be passed through to \code{\link[=hist]{hist()}},
or the \code{bw} and \code{kernel} arguments to \code{\link[=density]{density()}}.
The smooth density contour is drawn by default; to draw a binned plot,
use \code{smooth = FALSE}.
\subsection{Density vs Mass}{

Whether smoothed or binned, the height of each color at each
X coordinate represents the probability \strong{density} of each category.
The height of bars (when \code{smooth = FALSE}) does \emph{not} correspond exactly to the
the probability \strong{mass} in each bin, because that actually depends on the size of the bins;
For narrow bins (or continuous contours), density can even be greater than 1.
If bin sizes are all equal, then the \emph{relative} width of the density bars \emph{does}
map exactly to the relative probability mass of each bin.
If bins are \emph{not} equal width---which can only happen if you manually specify
unequal bins using the \code{breaks} argument---the widths of bars \emph{don't} map to probability mass.
However, using the density assures that the relative \strong{area} of each bin \emph{does} match the probability mass
associated with that bin, even if the bins are of unequal size.
The scale of the densities in the plot is shown through the width of a set of lines
to the left of the plot.
}
}
\section{Color}{


By default, colors are automatically chosen to represent
the categories in the \code{y} variable.
(A color legend will be drawn automatically.)
However, these colors can be controlled using the \code{col} argument;
you can provide a vector of unique colors exactly the same length as
the number of categories (unique values in \code{y}).
These colors are mapped to the areas, from bottom up.
(Use \code{alpha} independently to change the transparency.)
}

\section{General Draw Arguments}{


\code{draw()} is built on top of R's base plotting system.
This means that all the standard arguments to base-R plots can be used to customize \code{draw()} plots
(see \code{\link[=par]{par()}} for a full list).
You can also add stuff to \code{draw()} plots using base functions like \code{\link[=points]{points()}}, \code{\link[=mtext]{mtext()}}, or \code{\link[=abline]{abline()}}.
\code{draw()} also has a number of unique features, including easily dividing data into multiple plots ("facets").
\subsection{Plot Text}{

Every \code{draw()} plot can have a title, subtitle, X-axis label, and Y-axis label.

The \code{draw()} function will automatically generate X and Y labels for every plot,
usually just using the expression you passed; for example, if you say \code{draw(rnorm(100))}, the
X label will be "rnorm(100)."
This can be overridden using the \code{xlabe;} and/or \code{ylabe;} arguments, which can be provided a single string
each---to suppress a label, provide an empty string, like \code{ylabel = ""}.

Titles and subtitles are specified using the \code{title} and \code{subtitle} arguments, respectively.
(Alternatively, you can use the standard \code{main} and \code{sub} arguments.)
No title or subtitle is drawn by default.

You can use the base-R \code{\link[=mtext]{mtext()}} function to draw additional text on plot axes.
}

\subsection{Axes control}{

The \code{draw()} function will select reasonable X and Y axes ranges automatically.
If you want to override the defaults, you can use \code{xlimit} or \code{ylimit} to control
the range of values shown on each axis.
Each of these must be passed a vector of two numbers, representing the left and right
X-axis extremes (\code{xlimit}) and the bottom and top Y-axis extremes (\code{ylimit}).
For example, to show data in the range \eqn{[10, 50]} on the X axis,
specify \code{xlimit = c(10, 50)}.

For \code{numeric} axes, you can also plot data on a logarithmic scale
using the \code{log} argument.
This is set by providing a single \code{character} string containing a lower-case
\code{"x"}, \code{"y"}, or both (\code{"xy"}).
Note that \code{draw()} will throw an error if you try to plot negative values
on a logarithmic scale.
Also note that some plots will not allow logarithmic scaling on some axes,
and will simply ignore attempts to do that.
}

\subsection{Window control}{

Normally, when you use \code{draw()}, the plot is drawn to fill the available graphical device,
using base-R's normal algorithm.
However, the \code{aspect} argument can override this, controlling the aspect
ratio of the plot.
The \code{aspect} argument must be a single numeric value between \code{0.5} and \code{5}.
If \code{aspect = 1} the plot is drawn as a square.
Try settings like \code{aspect = 4/3} or \code{aspect = 16/9}.

The \code{margin} argument controls the portion of the screen used for the plot margins,
with legal values ranging from \code{0.4} to \code{0.1}.
The default value is \code{0.2}---using other margins may result in less optimal placement
of plot text and legends.
}
}

