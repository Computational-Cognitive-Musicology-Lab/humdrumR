% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{enum}
\alias{enum}
\title{Enumerate vector}
\usage{
enum(x, inPlace = TRUE, sep = ":")
}
\arguments{
\item{x}{\emph{\strong{The input vector to enumrate.}}

Must be a vector (either atomic, or a \code{list()}).}

\item{inPlace}{\emph{\strong{Should the numbers be pasted onto the original vector?}}

Defaults to \code{TRUE}.

Must be a singleton \code{logical} value: an on/off switch.}

\item{sep}{\emph{\strong{Separator between numbers and vector.}}

Defaults to \code{":"}.

Can be empty string (\code{""}), if no separator is desired.}
}
\description{
This function enumerates the values of an input vector \code{x},
counting along the length of the vector from 1 to \code{length(x)}.
}
\details{
If \code{inPlace = TRUE} (and \code{x} is atomic), the original vector is returned with the counts
pasted to the front of each value, separated by \code{sep}.
If \code{inPlace = FALSE}, a new \code{integer} vector is returned, identical to calling \link[base:seq]{seq_along(x)}.
}
\examples{

enum(letters)
enum(letters, inPlace = FALSE)

}
