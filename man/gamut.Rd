% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pitch.R
\name{gamut}
\alias{gamut}
\title{Make a pitch gamut}
\usage{
gamut(
  generic = FALSE,
  simple = FALSE,
  min.octave,
  max.octave,
  min.lof,
  max.lof,
  tints = NULL,
  deparser = kern,
  deparseArgs = list()
)
}
\arguments{
\item{generic}{Should the gamut include only generic intervals?

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: a on/off switch.}

\item{simple}{Should the gamut be constrained to one octave?

Defaults to \code{FALSE}.

Must be a singleton \code{logical} value: a on/off switch.}

\item{tints}{\emph{\strong{An optional reference vector to base the gamut on.}}

Defaults to \code{NULL}.

Must be a \code{NULL} or a \code{\link[=tonalInterval]{tonalInterval()}}.}

\item{deparser}{A \link[=pitchFunctions]{pitch function} to format the output.

Defaults to \code{\link[=kern]{kern()}}.

Must be a \link[=pitchFunctions]{pitch function}.}
}
\description{
This function generates a \href{https://en.wikipedia.org/wiki/Gamut}{gamut}:
a ordered range of notes.
It is used to generate \code{\link[=factor]{factor()}} levels for \link[=pitchFunctions]{pitch functions}.
}
