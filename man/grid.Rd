% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rhythm.R
\name{grid}
\alias{grid}
\alias{grid.default}
\alias{grid.humdrumR}
\alias{togrid}
\alias{fromgrid}
\alias{fromgrid.matrix}
\alias{fromgrid.character}
\alias{fromgrid.logical}
\alias{fromgrid.numeric}
\alias{fromgrid.integer}
\title{Drum-machine grid representation of rhythmic durations.}
\usage{
\method{grid}{default}(x, ..., parseArgs = list(), scale = 1, unit = 1, inPlace = FALSE)

humData |> select(Token) |> grid() 
humData |> grid(Token)

grid(x, ..., parseArgs = list(), scale = 1, unit = 1, inPlace = FALSE)

togrid(
  x,
  tick = tatum(x),
  measure = "1",
  on = "X",
  off = "O",
  collapse = TRUE,
  sep = ""
)

fromgrid(x, tick, meter)

\method{fromgrid}{matrix}(x, tick = "16")

\method{fromgrid}{character}(x, tick = "16", on = "X", off = "O", sep = "", deparser = recip, ...)

\method{fromgrid}{logical}(x, tick = "16", deparser = rint2recip, ...)

\method{fromgrid}{numeric}(x, tick = "16", deparser = recip, ...)

\method{fromgrid}{integer}(x, tick = "16", deparser = recip, ...)
}
\arguments{
\item{x}{\emph{\strong{An input  vector.}}

The \code{x} argument can be any (\link[base:vector]{atomic}) vector, or a \link[=rational]{rational (rhythmInterval)}, or \code{NULL}.

Must be \link[=rhythmParsing]{parsable as rhythm infromation}.}

\item{...}{\emph{\strong{Argments passed to the \link[=rhythmDeparsing]{deparser}.}}}

\item{parseArgs}{\emph{\strong{An optional list of arguments passed to the \link[=rhythmParsing]{rhythm parser}.}}

Defaults to an empty \code{list()}.

Must be a \code{list} of named arguments to the \link[=rhythmParsing]{rhythm parser}.}

\item{scale}{\emph{\strong{A \code{numeric} or \link{rational} value which is used as the output unit of measurement.}}

Defaults to \code{rational(1, 1)}.

Must be \code{numeric} or \link{rational}.}

\item{inPlace}{\emph{\strong{Should non-rhythm information be retained in the output string?}}

Defaults to \code{FALSE}.

Must be a singleton  \code{logical} value: an on/off switch.

Defaults to \code{FALSE}.

A singleton \code{logical} value, or \code{NA}.

See "Grace notes" section below.}

\item{tick}{\emph{\strong{What is the unit of the grid?}}

Defaults to sixteenth-note (\code{fromgrid()}) or the \code{\link[=tatum]{tatum()}} of the \code{x} argument.

Must be parsed as a rhythm by \code{\link[=rhythmInterval]{rhythmInterval()}}.}

\item{on, off}{\emph{\strong{What represents onsets (attacks) and rests in the grid?}}

Default to \code{"X"} and \code{"O"} respectively.

Must be singleton atomic values.}

\item{collapse}{\emph{\strong{Should the output be collapsed to a single string per measure?}}

Defaults to \code{TRUE}.

Must be singleton \code{logical} value: an on/off switch.}

\item{sep}{\emph{\strong{Separator between ticks in collapsed output.}}

Defaults to an empty string (no separator).

Must be a singleton \code{character} string.}

\item{deparser}{\emph{\strong{What output representation should be returned?}}

Defaults to \code{\link[=recip]{recip()}}.

Must be a function which accepts \code{\link[=rational]{rational()}} numbers.}
}
\description{
These functions read and write a sequencer-like representation of rhythm.
Rhythms are represented as either strings or vectors of "on"/"off" values,
which indicate where rhythmic onsets occur in a regular time grid.
For example, \code{"X00X00X0"} or \code{c(1, 0, 0, 1, 0, 0, 1, 0)}.

If \code{grid()} is applied to a \link[=humdrumRclass]{humdrumR data class}
you may use the data's \link[=fields]{fields} as arguments.
If no field names are specified, the first \link{selectedField} is used as \code{x}.
}
\details{
The \code{grid()} function, is a fully vectorized \link[=rhythmFunctions]{rhythm function}, which translates
\emph{individual} durations to a grid-representation strings.
For example, on a 16th-note grid, a dotted eighth-note would be represented \code{"XOO"}.
The \code{fromgrid()} and \code{togrid()} functions create/read fuller
grid representations, representing whole rhythms at once: in their case,
the length of input and output will not be the same.
}
\examples{

rhythm <- c('8.', '8.', '8', '8.', '8', '16', '8')

grid(rhythm)

togrid(rhythm, on = '1', off = '0')

togrid(rhythm, collapse = FALSE)

fromgrid('XOXOXOOXXOXOXXOO', tick = '8')


}
\seealso{
To better understand how \code{grid()} works,
read about the \link[=rhythmFunctions]{family of rhythm functions},
or how rhythms are \link[=rhythmParsing]{parsed} and \link[=rhythmDeparsing]{deparsed}.

Other {rhythm functions}: 
\code{\link{duration}()},
\code{\link{notehead}()},
\code{\link{recip}()}
}
\concept{{rhythm functions}}
