# Combine one or more fields into a new field

Combine one or more fields into a new field

## Usage

``` r
combineFields(humdrumR, ...)
```

## See also

You can do similar things using [subset.humdrumR() and
complement()](https://humdrumR.ccml.gtcmt.gatech.edu/reference/subset.humdrumR.md).
